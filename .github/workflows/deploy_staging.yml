name: deploy staging
on:
  push:
    branches:
      - 'staging'
env:
  SALUT: 'salut'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{secrets.JWT_SECRET}}
      - run: echo ${{env.SALUT}}